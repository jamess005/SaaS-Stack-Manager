# PyTorch (ROCm) — install separately before other deps:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm7.2

# ML / inference
transformers>=4.45.0
peft>=0.13.0
datasets>=2.20.0
accelerate>=0.34.0
bitsandbytes>=0.44.0
huggingface-hub>=0.24.0
rich>=13.7.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting / formatting (local dev; also used by pre-commit)
black>=24.0.0
isort>=5.13.0
flake8>=7.0.0
pre-commit>=3.7.0
